@model IEnumerable<_2BHLM.Models.MarketEntities.StoredCar>

@{
    ViewBag.Title = "GIỎ HÀNG";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>GIỎ HÀNG</h2>

<p>
    @Html.ActionLink("THÊM HÀNG", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Titleofproduct)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AmmountofProduct)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Titleofproduct)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AmmountofProduct)
        </td>
        <td>
            @Html.ActionLink("THÊM HÀNG", "Edit", new { id=item.Idstore }) |
            @Html.ActionLink("KIỂM TRA THÔNG TIN", "Details", new { id=item.Idstore }) |
            @Html.ActionLink("HỦY BỎ", "Delete", new { id=item.Idstore })
        </td>
    </tr>
}

</table>
